services:
  app:
    container_name: network-master-hahbr88
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:80"
  data-builder:
    build:
      context: .
      dockerfile: docker/data-builder.Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
